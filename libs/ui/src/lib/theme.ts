import { createTheme } from '@mantine/core';

export const dashboardTheme = createTheme({
  primaryColor: 'blue',
  // In the future we will map PandaCSS tokens here
  // and define specific surface elevations for the light theme
});
